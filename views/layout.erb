<!doctype html>
<html lang="en-US">
  <head>
    <title>CMS App</title>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="//fonts.googleapis.com/css?family=PT+Sans">
  </head>
  <body>
    <header>
    </header>

    <% if session[:message] %>
      <p><%= session.delete(:message) %></p>
    <% end %>

    <main>
      <%= yield %>
    </main>
  </body>
</html>
